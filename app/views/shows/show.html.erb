<%= @show.show_title %></h2><br>
<p><%= link_to "Edit", edit_show_path %></p>
  <%= button_to "Delete", show_path(@show), method: :delete, data: { confirm: "Really?" } %>
 <p><%= link_to "Add Note", new_show_note_path(@show.id) %></p>