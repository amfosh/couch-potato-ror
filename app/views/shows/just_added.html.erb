<h2>Your Watchlist by most recently added:</h2>

   <% @shows.each do |s| %>
   <h4><li><%= link_to s.show_title, show_path(s.id) %></h4>
    <p><%= link_to "Show Notes", show_notes_path(s.id) %>
<% end %>