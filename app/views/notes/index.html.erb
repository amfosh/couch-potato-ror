<h1>All Notes </h1>
<ul>
<% @notes.each do |note| %>

     <li>
        <h3><%= link_to note.show.show_title, show_path(note.show) %></h3>
        <p><%= note.content %>

<% end %>
</ul>
<p>Add a new note <%= link_to "here.", new_show_note_path %></p> 